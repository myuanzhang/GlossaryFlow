# 开发环境设置指南

## ⚠️ 重要说明

如果你在 IDE 中看到以下错误提示：
- `找不到模块"react"或其相应的类型声明`
- `找不到模块"@ant-design/icons"`
- `找不到模块"./style.css"`

**这是正常现象！** 原因是：
- 项目已经删除了 `node_modules/` 和 `venv/` 目录以减小体积
- 这些依赖会在用户首次安装时自动下载
- TypeScript 错误提示会在安装依赖后消失

## 快速解决方法

### 方法 1：安装前端依赖（推荐）

```bash
cd frontend
npm install
```

安装完成后，TypeScript 错误提示会自动消失。

### 方法 2：忽略 TypeScript 错误（临时）

如果你只是想查看代码，暂时不想安装依赖，可以忽略这些错误提示，不会影响代码功能。

## 完整开发环境设置

### 1. 安装前端依赖

```bash
cd frontend
npm install
```

这将安装所有必需的依赖，包括：
- `react` - React 框架
- `@ant-design/icons` - Ant Design 图标库
- 以及其他所有依赖

### 2. 安装后端依赖

```bash
# 创建虚拟环境
python3 -m venv venv

# 激活虚拟环境
source venv/bin/activate  # macOS/Linux
# 或
venv\Scripts\activate  # Windows

# 安装 Python 依赖
pip install -r requirements.txt
```

### 3. 配置环境变量

```bash
# 复制配置模板
cp .env.example .env

# 编辑 .env 文件，填入你的 API Keys
# 至少配置一个 Provider
```

### 4. 启动开发服务器

**启动后端**：
```bash
python main.py
```

**启动前端**（新终端）：
```bash
cd frontend
npm run dev
```

### 5. 访问应用

打开浏览器访问 http://localhost:5173

## 项目结构说明

```
document-translation/
├── frontend/
│   ├── src/
│   │   └── components/
│   │       └── ProgressDisplay/
│   │           ├── index.tsx       # 组件代码
│   │           ├── style.css       # 基础样式
│   │           └── enhanced.css    # 增强样式
│   ├── node_modules/               # ❌ 已删除（通过 npm install 安装）
│   ├── package.json                # ✅ 依赖配置文件
│   └── ...
├── src/                            # ✅ 后端源代码
├── venv/                           # ❌ 已删除（通过 python -m venv 创建）
└── requirements.txt                # ✅ Python 依赖配置
```

## 为什么删除 node_modules 和 venv？

1. **减小体积**：
   - `node_modules/` 约 256 MB
   - `venv/` 约 56 MB
   - 删除后项目从 314 MB 减少到 1.7 MB（减少 99.5%）

2. **避免平台冲突**：
   - macOS 的 node_modules 不能在 Windows 上使用
   - Python 虚拟环境包含平台特定的二进制文件

3. **Git 最佳实践**：
   - 依赖配置文件（`package.json`, `requirements.txt`）已包含所有信息
   - 用户可以通过这些配置文件重新安装依赖
   - 避免提交二进制文件和大量第三方代码

## 验证依赖安装成功

### 前端依赖检查
```bash
cd frontend
npm list react
npm list @ant-design/icons
```

如果显示版本号，说明安装成功。

### 后端依赖检查
```bash
source venv/bin/activate  # 或 venv\Scripts\activate (Windows)
pip list | grep fastapi
pip list | grep openai
```

如果显示版本号，说明安装成功。

## 常见问题

### Q: 为什么 npm install 很慢？
A: 可以使用国内镜像加速：
```bash
npm install --registry=https://registry.npmmirror.com
```

### Q: 为什么 pip install 失败？
A: 可以使用国内镜像加速：
```bash
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q: TypeScript 错误提示还在？
A: 尝试以下操作：
1. 重启 IDE
2. 清除 IDE 缓存
3. 运行 `npm run build` 检查是否有真正的错误

### Q: 可以提交 node_modules 到 Git 吗？
A: **强烈不建议**！理由：
- 体积巨大，增加克隆时间
- 包含平台特定的二进制文件
- 可以通过 `npm install` 轻松恢复
- Git 仓库应该只包含源代码

## 总结

- ❌ **删除的目录**：`node_modules/`, `venv/` - 依赖库
- ✅ **保留的文件**：`package.json`, `requirements.txt` - 依赖配置
- ✅ **功能完整**：所有源代码和配置文件都保留
- 📦 **自动安装**：用户通过 `npm install` 和 `pip install` 即可恢复依赖

这些 TypeScript 错误提示**不影响代码功能**，只是 IDE 提示缺少依赖模块，安装依赖后会自动消失。
